<section class="m-t-1">
  <label>Limit</label>
  <select class="form-control">
    {{#each limits}}
    <option value="{{value}}" {{#if selected}}selected{{/if}}>{{ display }}</option>
    {{/each}}
  </select>
  <input {{#unless showOther}}style="display:none;"{{/unless}} type="number" class="form-control" value="{{ currentLimit }}">
</section>

<section class="m-t-1">
  <label>Color</label>
  <ul class="color-picker">
    {{#each colors}}
    <li class="{{#if noColor}}no-color{{/if}} {{#if selected}}selected{{/if}}"
        style="background-color:{{color}}"
        data-color="{{color}}">{{#if noColor}}None{{/if}}</li>
    {{/each}}
  </ul>
</section>
